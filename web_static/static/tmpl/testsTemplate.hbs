<div class="container">
  <h1>Tests</h1>
  <a {{action "reload"  on="click"}}><i class="icon-refresh icon-large"></i></a>
  <ul class="nav nav-tabs">
    <li class="active">
      <a href="#">Mine</a>
    </li>
    <li><a href="#">by Tag</a></li>
    <li><a href="#">All</a></li>
  </ul>
  <table class="table table-bordered table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Status</th>
        <th>Task</th>
        <th>Started at</th>
        <th>Ended at</th>
        <th>Tags</th>
      </tr>
    </thead>
    <tbody>
      {{#each item in controller}}
        <tr>
          <td>{{#linkTo test item}}{{item.title.test_name}}{{/linkTo}}</td>
          <td>{{item.status_name}}</td>
          <td>{{item.title.task}}</td>
          <td>{{item.started_at_pp}}</td>
          <td>{{item.ended_at_pp}}</td>
          <td>
            {{#each tag in item.tags}}
              <span class="label label-info">{{tag}}</span>
            {{/each}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
  <ul class="pager">
    <li {{bindAttr class=":next olderDisabled:disabled"}}>
      {{#unless olderDisabled}}
        <a {{action "goToPage" nextStr}} href="next">Older &rarr;</a>
      {{else}}
        <a>Older &rarr;</a>
      {{/unless}}
    </li>
    <li {{bindAttr class=":previous newerDisabled:disabled"}}>
      {{#unless newerDisabled}}
        <a {{action "goToPage" prevStr}} href="prev">&larr; Newer</a>
      {{else}}
        <a>&larr; Newer</a>
      {{/unless}}
    </li>
  </ul>
</div>
